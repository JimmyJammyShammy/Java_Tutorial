package jimmy;
// Naming convention, capital I followed by the name with able
public interface IGeneratable {

    // If a cave implements the IGeneratable interface,
    // It is automatically known it can call the generate method
    void generate();

}
